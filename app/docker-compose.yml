
services:
  backend:
    build: ./backend
    container_name: kiosk-backend
    ports:
      - "4000:4000"

  frontend:
    build: ./frontend
    container_name: kiosk-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
